# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: techne

before:
  hooks:
    - go vet ./...

builds:
  - id: techne
    dir: .
    main: ./cmd/techne
    binary: techne
    # CGO is required by pkg/lang/rust (github.com/smacker/go-tree-sitter
    # bundles the tree-sitter C library). Cross-compilers are provided by
    # the goreleaser-cross Docker image used in .github/workflows/
    # release.yml — osxcross for darwin (with the macOS SDK), llvm-mingw
    # for windows, and the host glibc/musl toolchains for linux. Per-
    # target CC/CXX overrides below select the right cross compiler.
    env:
      - CGO_ENABLED=1
    mod_timestamp: "{{ .CommitTimestamp }}"
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    overrides:
      - goos: linux
        goarch: amd64
        env:
          - CC=x86_64-linux-gnu-gcc
          - CXX=x86_64-linux-gnu-g++
      - goos: linux
        goarch: arm64
        env:
          - CC=aarch64-linux-gnu-gcc
          - CXX=aarch64-linux-gnu-g++
      - goos: darwin
        goarch: amd64
        env:
          - CC=o64-clang
          - CXX=o64-clang++
      - goos: darwin
        goarch: arm64
        env:
          - CC=oa64-clang
          - CXX=oa64-clang++
      - goos: windows
        goarch: amd64
        env:
          - CC=x86_64-w64-mingw32-gcc
          - CXX=x86_64-w64-mingw32-g++
      - goos: windows
        goarch: arm64
        env:
          - CC=/llvm-mingw/bin/aarch64-w64-mingw32-gcc
          - CXX=/llvm-mingw/bin/aarch64-w64-mingw32-g++
    ldflags:
      - -s -w
      - -X go.thesmos.sh/techne/internal/version.buildVersion={{.Version}}
      - -X go.thesmos.sh/techne/internal/version.buildCommit={{.Commit}}
      - -X go.thesmos.sh/techne/internal/version.buildDate={{.CommitDate}}

archives:
  - id: default
    formats: ["tar.gz"]
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        formats: ["zip"]

source:
  enabled: true
  name_template: "{{ .ProjectName }}_{{ .Version }}_source"

checksum:
  name_template: checksums.txt
  algorithm: sha256

signs:
  - cmd: cosign
    signature: "${artifact}.sig"
    certificate: "${artifact}.pem"
    args:
      - sign-blob
      - "--output-signature=${signature}"
      - "--output-certificate=${certificate}"
      - "${artifact}"
      - "--yes"
    artifacts: checksum
    output: true

changelog:
  sort: asc
  use: github
  groups:
    - title: New features
      regexp: "^feat"
      order: 0
    - title: Performance
      regexp: "^perf"
      order: 1
    - title: Bug fixes
      regexp: "^fix"
      order: 2
    - title: Test rigor
      regexp: "^test"
      order: 3
    - title: Refactors
      regexp: "^refactor"
      order: 4
    - title: Other
      order: 999
  filters:
    exclude:
      - "^docs"
      - "^ci"
      - "^chore"
      - "^build"
      - "^style"
      - "Merge pull request"
      - "Merge branch"

homebrew_casks:
  - name: techne
    # CLI binary inside the cask — goreleaser unpacks the archive
    # and links binaries[0] into Homebrew's bin/.
    binaries:
      - techne
    homepage: "https://github.com/thesm-os/techne"
    description: "Task runner for Go projects: bootstrap, format, lint, test, bench, release, and a multi-stage check umbrella."
    # Tap repo lives at github.com/thesm-os/homebrew-tap.
    # Cross-repo writes need a PAT with contents:write on the tap;
    # the release workflow exposes it as $HOMEBREW_TAP_TOKEN.
    repository:
      owner: thesm-os
      name: homebrew-tap
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    commit_author:
      name: goreleaser-bot
      email: bot@thesmos.sh
    commit_msg_template: "brew: bump {{ .ProjectName }} to {{ .Tag }}"
    # Skip prereleases (anything with a `-` suffix, e.g. v1.0.0-rc1).
    skip_upload: auto

release:
  github:
    owner: thesm-os
    name: techne
  draft: false
  prerelease: auto
  footer: |
    **Full changelog:** https://github.com/thesm-os/techne/compare/{{ .PreviousTag }}...{{ .Tag }}

report_sizes: true
